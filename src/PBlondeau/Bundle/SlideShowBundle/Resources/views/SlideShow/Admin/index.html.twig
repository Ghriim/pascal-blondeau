{% extends "::adminLayout.html.twig" %}

{% trans_default_domain "adminSlideShow" %}

{% block body %}
     <div class="row">
        <div class="col-md-10">
            {{ 'list.slide.count' | trans({'%numberOfSlide%': slides.getTotalItemCount}) }}
        </div>

        <div class="col-md-2 text-right">
            <button class="btn btn-default" data-toggle="modal" data-target="#add-form">
                {{ 'list.action.add' | trans({}) }}
            </button>
        </div>
    </div>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>{{ 'list.slide.header.image' | trans({}) }}</th>
                <th>{{ 'list.slide.header.status' | trans({}) }}</th>
                <th>{{ 'list.slide.header.creation' | trans({}) }}</th>
                <th>{{ 'list.slide.header.actions' | trans({}) }}</th>
            </tr>
        </thead>

        <tbody>
            {% for slide in slides %}
                <tr>
                    <td><img src="{{ slide.path }}" alt="" /></td>
                    <td>
                        <span class="badge status-{{ slide.status }}">
                            {{ ('list.slide.body.status.' ~ slide.status) | trans({}) }}
                        </span>
                    </td>
                    <td>{{ slide.creation|date('d-m-Y') }}</td>
                    <td>
                        <ul class="list-inline">
                            <li>
                                <a href="" title="{{ 'list.slide.body.action.edit' | trans({}) }}">
                                    <span class="fa fa-pencil"></span>{{ 'list.slide.body.action.edit' | trans({}) }}</a>
                            </li>

                            <li>
                                <a href="" title="{{ 'list.slide.body.action.delete' | trans({}) }}">
                                    <span class="fa fa-times-circle"></span>{{ 'list.slide.body.action.delete' | trans({}) }}</a>
                            </li>
                        </ul>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4" class="">{{ 'list.slide.empty' | trans({}) }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(slides) }}
    </div>

    <div id="add-form" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true">
        {% include "PBlondeauSlideShowBundle:SlideShow/Admin:form.html.twig" with {'form': form} only %}
    </div>
{% endblock %}

{% extends '::layout.html.twig' %}

{% block body %}
    <section id="exhibtions">
        {% for exhibition in exhibitions %}
            <article class="text-center margin-bottom">
                <img class="img-max-width" src="{{ asset(exhibition.webPath) }}" alt="" />
            </article>
        {% endfor %}
    </section>

    <div class="paginator">
        {{ knp_pagination_render(exhibitions) }}
    </div>
{% endblock %}